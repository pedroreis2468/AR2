[pytest]
# Recolher apenas a suite unitaria em tests/.
# Evita que `pytest` (sem argumentos) tente importar os scripts de debug/smoke
# em scripts/ (test_steering.py, test_pacsim_*.py), que executam codigo no
# import e nao sao testes pytest.
testpaths = tests
