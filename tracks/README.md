# Pistas Formula Student

Pistas de competição FS em formato YAML obtidas do repositório [PacSim](https://github.com/pacsim/pacsim).

As pistas não-circuito (`skidpad`, `acceleration`, `gripMap`) são excluídas automaticamente do treino pelo `YAMLTrackLoader`.
